form-item() {
  font-size: 13px;
  box-sizing: border-box;
  border: 1px solid #ddd;
  border-radius: 2px;
  color: #333;
}

plus-icon(color) {
  display: inline-block;
  content: "+"
  font-size: 24px;
  color: color;
  font-weight: bold;
  transform: translateY(1px);
}
